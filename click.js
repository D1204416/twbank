document.addEventListener('DOMContentLoaded', function () {
    // 獲取按鈕元素
    var btnLogon = document.getElementById('ccbtnLogon');

    // 添加點擊事件監聽器
    btnLogon.addEventListener('click', function () {
        // 彈出alert顯示資訊錯誤
        alert('資訊錯誤');
    });
});
